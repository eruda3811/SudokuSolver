#!/bin/bash
# Double-click this file to open the Sudoku Solver in your default browser.
# Works fully offline — the app is a single self-contained HTML file.
DIR="$(cd "$(dirname "$0")" && pwd)"
open "$DIR/index.html"
